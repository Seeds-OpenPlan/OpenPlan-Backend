# 힙 상한 — 기본값이 낮은 환경에서 테스트 워커가 죽는 것을 막는다.
org.gradle.jvmargs=-Xmx2g

# ✅ 해소됨 — 한글 경로 제약 (2026-07-18, ST-B1-01b → D-02에서 해소)
#   과거 증상: 저장소가 한글 경로(...\OneDrive\바탕 화면\OpenPlan\...) 아래에 있으면
#   `gradlew test`가 ClassNotFoundException으로 한 건도 실행되지 않았다.
#   컴파일(compileJava/compileTestJava)은 정상이라 눈에 잘 안 띄었다.
#
#   원인: Windows JVM의 sun.jnu.encoding=MS949 + Gradle 테스트 워커의 클래스패스 전달.
#         sun.jnu.encoding은 JVM 기동 후 변경 불가라 -Dfile.encoding=UTF-8로는 해결되지 않는다(실측).
#
#   해소: D-02에서 저장소를 C:\dev\openplan(ASCII 경로)으로 이전.
#   실측(2026-07-18, 이전 후): BUILD SUCCESSFUL, 6/6 통과(contextLoads 포함).
#
#   ⚠️ 따라서 이 저장소를 다시 한글/공백 경로로 옮기지 말 것. 증상이 재발한다.
